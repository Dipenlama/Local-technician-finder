name: mistrix_backend
description: REST API backend for the Mistrix local technician finder.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.7.0

dependencies:
  bcrypt: ^1.2.0
  dart_jsonwebtoken: ^3.4.1
  shelf: ^1.4.2
  shelf_router: ^1.1.4

dev_dependencies:
  http: ^1.5.0
  lints: ^5.1.1
  test: ^1.25.15
